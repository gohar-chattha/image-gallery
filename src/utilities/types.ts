export interface IColors {
  black: string;
  white: string;
  blue: string;
  grey: string;
}
export interface IFontSizes {
  p: number;
}
export interface ImagesResponseFormat {
  message: string[];
  status: string;
}
